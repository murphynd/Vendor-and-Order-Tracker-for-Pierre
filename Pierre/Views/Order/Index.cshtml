@using MusicOrganizer.Models;
@{
  Layout = "_Layout";
}

<h1 class="jumbotron">orders</h1>

@if (@Model.Count == 0)
{
  <h3>No orders have been added yet!</h3>
} 

@foreach (order order in Model)
{
  <h3><a href='/orders/@order.Id'>@order.Title</a></h3>
}

<p><a href='/'>Back home</a></p>
